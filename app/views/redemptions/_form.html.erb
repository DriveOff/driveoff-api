<%= form_for(@redemption) do |f| %>
  <% if @redemption.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@redemption.errors.count, "error") %> prohibited this redemption from being saved:</h2>

      <ul>
      <% @redemption.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
