{
  "name": "driveoff",
  "scripts": {
  },
  "env": {
    "AWS_ID": {
      "required": true
    },
    "AWS_REGION": {
      "required": true
    },
    "AWS_SECRET": {
      "required": true
    },
    "S3_BUCKET": {
      "required": true
    }
  },
  "addons": [
    "heroku-postgresql"
  ],
  "buildpacks": [
    {
      "url": "urn:buildpack:heroku/ruby"
    }
  ]
}
