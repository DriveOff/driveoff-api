{
  "name": "driveoff",
  "scripts": {
  },
  "env": {
    "AWS_ID": {
      "required": true
    },
    "AWS_REGION": {
      "required": true
    },
    "AWS_SECRET": {
      "required": true
    },
    "S3_BUCKET": {
      "required": true
    },
  },
  "formation": {
  },
  "addons": [

  ],
  "buildpacks": [

  ]
}
